{% extends "SellermaniaTestBundle::layout.html.twig" %}

{% block page_content %}
    <h1>Add comment for idea n°{{ idea.id }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="btn btn-success"/>
    {{ form_end(form) }}

{% endblock %}
